// Shared, framework-agnostic configuration values/types used by both
// apps/web and apps/api. Populated once a concrete cross-app config need
// exists; kept empty rather than speculative for this checkpoint.

export const CONFIG_PACKAGE_NAME = "@bmp/config";
