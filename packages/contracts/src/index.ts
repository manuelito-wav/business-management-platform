// Shared transport and validation contracts between apps/web and apps/api
// (see ARCHITECTURE.md "contracts"). Populated once the first REST DTOs
// and the versioned command envelope are defined (Phase 0/1).

export const CONTRACTS_PACKAGE_NAME = "@bmp/contracts";
