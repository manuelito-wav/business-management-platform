node_modules
dist
.next
.turbo
coverage
**/generated/prisma
pnpm-lock.yaml
*.tsbuildinfo

# Local worktrees for background/spawned Claude Code sessions -- not
# tracked by this repo (.gitignore), but Prettier does not read
# .git/info/exclude, so it needs its own explicit ignore entry too.
.claude/worktrees/

# Governance/spec docs use deliberate, hand-maintained structure (see
# CODESTYLE.md "Documentation": targeted edits only, never bulk rewrites).
*.md
