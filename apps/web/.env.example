# Development: copy to `.env.local` in this directory and adjust values.
# Staging / Production: injected by the hosting platform, not read from a file.

NODE_ENV=development
PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:3001
