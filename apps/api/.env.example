# Development: copy to `.env` in this directory and adjust values.
# Staging / Production: injected by the hosting platform, not read from a file.

NODE_ENV=development
PORT=3001
DATABASE_URL="postgresql://bmp:bmp_dev_password@localhost:5432/bmp_dev?schema=public"
WEB_ORIGIN=http://localhost:3000
